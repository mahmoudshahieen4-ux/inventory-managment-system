# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.local.*
jscpd-report

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri updater signing keys — NEVER commit private keys to the repository.
# Store them in GitHub Secrets only (TAURI_PRIVATE_KEY / TAURI_KEY_PASSWORD).
*.key
*.pem
~/.tauri/
